spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    defer-datasource-initialization: true

  sql:
    init:
      mode: never
      data-locations: classpath:/dml.sql

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mysql
    username: root
    password:

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      url: jdbc:redis://${REDIS_USER}:${REDIS_PASSWORD}@${REDIS_HOST}:${PORT}
      password: ${REDIS_PASSWORD}

app:
  token:
    access:
      secret-key: L4xO5TLWQzVLDdXb5cyOzDPostvp1YOa
      expiration: 99999999
    refresh:
      expiration: 99999999


oauth:
  kakao:
    client-id: 490d4b77ebf8180c237157b47e6185b1
    url:
     auth: https://kauth.kakao.com
     api: https://kapi.kakao.com

location:
  kakao:
    api-key: ${KAKAO_REST_API_KEY}
    search-api: https://dapi.kakao.com/v2/local/search/address
    coord-api: https://dapi.kakao.com/v2/local/geo/coord2regioncode
